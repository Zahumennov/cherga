# Copy to .env (already gitignored) and fill in. Never commit real values.

# Deployer key for testnet/mainnet deploys via script/Deploy.s.sol.
# Use a dedicated throwaway key, not a wallet holding real funds.
PRIVATE_KEY=
